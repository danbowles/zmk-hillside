on: [push, pull_request, workflow_dispatch]

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@main
    with:
      # This workflow expects the path to the folder
      # containing your build.yaml and keymap
      config_path: "config"
